帮我做一个防手机桌面的ai聊天软件，移动
端优先。首先，页面顶部是和真实手机一样
的时间、日期、天气、所在城市板块，且该
板块可以感知现实时间和日期，天气和所在
城市用户可以自主设置，以上所有在聊天中
可以被api读取。该板块有单独的底部背景，
底部背景在类似云朵的模块内。其次，页面
底部有设置、美化、预设三个图标。用
openai格式，设置里面可以填写baseurl,api
key,model name，点击保存生效，用这些
参数去调api。预设里可以通过加入css代码
设置回复文风和规定回复规则，包括回复限
制词等。美化里可以通过加入css代码改变图
标样式，整体背景图片，各板块背景图片。
最后，在顶部板块下面，设置私聊图标，点
进去是仿微信对话界面，用户输入消息后显
示在屏幕上，用户可输入多条消息，有一个
意成按钮，点击生成后把聊天记录发送到

test

城市用产可以目王设直，以工所有在聊大中
可以被api读取。该板块有单独的底部背景，
底部背景在类似云朵的模块内。其次，页面
底部有设置、美化、预设三个图标。用
openai格式，设置里面可以填写baseurl,api
key,model name，点击保存生效，用这些
参数去调api。预设里可以通过加入css代码
设置回复文风和规定回复规则，包括回复限
制词等。美化里可以通过加入css代码改变图
标样式，整体背景图片，各板块背景图片。
最后，在顶部板块下面，设置私聊图标，点
进去是仿微信对话界面，用户输入消息后显
示在屏幕上，用户可输入多条消息，有一个
生成按钮，点击生成后把聊天记录发送到
api，获得对方的回答，对方可以根据前五次
的对话生成回复，回复可以是多条，仿真人
微信聊天，每条回复不需要太长，每条至多
20字。帮我写一个适合大模型的prompt来调